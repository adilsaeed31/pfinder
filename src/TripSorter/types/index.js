export type DealsType = {
arrival: string,
    cost: number,
    departure: string,
    discount: number,
    duration: {
      h: string,
      m: string,
    },
    reference: string,
    transport: string,
}